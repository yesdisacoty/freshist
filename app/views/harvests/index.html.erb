index<br>
<% @harvests.each do |harvest| %>
    <%= image_tag harvest.harvest_photos %>
    <%= link_to harvest.name, harvest %>
    <%= "#{harvest.user.name}" %>
    <%= harvest.price %>
    <%= harvest.description %>
    <%= link_to "Buy", new_harvest_order_path(harvest) %>
    <% if harvest.user == current_user %>
    <%= link_to "Show", harvest %>
    <%= link_to "Edit", edit_harvest_path(harvest) %>
    <%= link_to "delete", harvest, method: :delete, data: { confirm: "You sure?" } %>
    <% end %>
<% end %>